[pytest]
markers =
    integration: live network tests (skipped by default; run with -m integration)
    e2e: end-to-end tests requiring mock/docker services (skipped by default; run with -m e2e)
addopts = -m "not integration and not e2e"
testpaths = tests
